LLM（私など）に対して、一度に全ての要件を投げるとコンテキストが複雑になりエラーが起きやすいため、実装手順を**4つのステップ（タスク）**に分割しました。

これらを順番にコピー＆ペーストして指示を出していくことで、スムーズに実装を進めることができます。

---

### タスク 1: プロジェクトの初期化と環境構築

まず、土台となる環境と設定ファイルを作成させます。

**プロンプト:**

```markdown
# タスク: Vite + React プロジェクトの初期設定

以下の要件に従って、新しいWebアプリケーションの初期設定ファイルの内容を作成してください。

## 技術スタック
- Vite (React、TypeScript)
- Tailwind CSS (スタイリング用)
- @google/generative-ai (Gemini API用)

## 実行してほしいこと
1. 必要なnpmパッケージのインストールコマンドを教えてください。
2. `tailwind.config.js` と `index.css` の設定コードを提示してください。
3. プロジェクトのルートに `.env` ファイルを作成し、Gemini APIキーを格納する変数を定義する例（`VITE_GEMINI_API_KEY`）を示してください。

この段階ではコンポーネントの実装は不要です。設定ファイルのみ出力してください。

```

---

### タスク 2: Gemini API連携サービスの作成

次に、バックエンドロジックとなるAPI通信部分を作成させます。

**プロンプト:**

```markdown
# タスク: Gemini API連携サービスの作成

`src/services/gemini.js` ファイルを作成し、画像をGeminiに送信してバウンディングボックスを取得する関数を実装してください。

## 要件
1. **ライブラリ:** `@google/generative-ai` を使用する。
2. **関数名:** `extractTextFromImage(file, apiKey, targetText = '')`
3. **処理内容:**
   - 画像ファイル（Fileオブジェクト）をBase64などのAPIに適した形式に変換する。
   - 以下のプロンプトを使用して `generateContent` を呼び出す。
     > "Return a JSON list of text found in this image with bounding box coordinates [ymin, xmin, ymax, xmax] normalized to 0-1000. If '${targetText}' is provided, only return matches."
   - レスポンス（JSON文字列）をパースしてオブジェクトとして返す。
4. **エラーハンドリング:** APIキーがない場合や通信エラー時の処理を含める。

UIの実装はまだ不要です。このロジックファイルのみ作成してください。

```

---

### タスク 3: UIコンポーネントの作成 (アップローダーと結果表示)

主要な2つのコンポーネントを作成させます。特に「結果表示」は座標変換のロジックが必要なので丁寧に指示します。

**プロンプト:**

```markdown
# タスク: UIコンポーネントの実装

以下の2つのReactコンポーネントを作成してください。

## 1. `src/components/ImageUploader.jsx`
- ユーザーが画像をドラッグ＆ドロップ、またはクリックして選択できるエリア。
- 選択された画像のプレビューを表示する。
- 選択されたファイルを親コンポーネントに渡すコールバックを持つ。

## 2. `src/components/ResultViewer.jsx`
- **Props:** `imageUrl` (画像URL), `results` (Geminiから返ってきたJSONデータ)
- **レイアウト:** 左右2カラム（左：テキストリスト、右：画像表示）。
- **画像表示エリア (右):**
  - 画像を表示し、その上にバウンディングボックス（SVGまたはCanvas）を重ねて描画する。
  - **重要:** Geminiの座標は `0-1000` で正規化されているため、実際の画像の表示サイズ（ピクセル）に合わせて座標変換を行うこと。
- **リスト表示エリア (左):**
  - 抽出されたテキストをリスト表示する。
- **インタラクション:**
  - リストの項目をホバーしたら、対応する画像のボックスをハイライトする（逆も同様）。

スタイリングにはTailwind CSSを使用してください。

```

---

### タスク 4: App.jsxでの統合と状態管理

最後に、これらを `App.jsx` で繋ぎ合わせます。

**プロンプト:**

```markdown
# タスク: アプリケーションの統合 (App.jsx)

作成したコンポーネントとサービスを統合して `src/App.jsx` を完成させてください。

## 要件
1. **状態管理:**
   - APIキー（入力フィールド または `.env` からの読み込み）
   - アップロードされた画像ファイル
   - 抽出結果データ
   - ローディング状態
   - ターゲットテキスト（絞り込み用ワード）の入力状態
2. **UI構成:**
   - ヘッダー：タイトルとAPIキー入力欄。
   - メインエリア：
     - 画像未選択時：`ImageUploader` を表示。
     - 画像選択時：
       - 「ターゲットテキスト」入力欄と「実行ボタン」を表示。
       - 実行ボタン押下で `extractTextFromImage` を呼び出す。
       - 結果取得後、`ResultViewer` を表示する。
3. **UX:**
   - ローディング中はスピナーや「解析中...」等の表示を行うこと。

Tailwind CSSを使って見やすくレイアウトしてください。

```